# PCA-Enhanced Adaptive NVAR Framework for High-Resolution Sea Surface Temperature Forecasting

This repository contains the computational notebooks and supporting materials for:

**PCA-Enhanced Adaptive NVAR Framework for High-Resolution Sea Surface Temperature Forecasting**

The implementation applies principal component analysis (PCA) to sea-surface-temperature (SST) fields and uses an adaptive nonlinear vector autoregression (Adaptive NVAR) model for forecasting. Standard NVAR/NG-RC experiments and dataset-preparation notebooks are also provided for comparison and reproducibility.

## Repository structure

```text
.
├── README.md
├── LICENSE
├── requirements.txt
├── .gitignore
├── examples/
│   ├── README.md
│   ├── SST_normalize.ipynb
│   ├── Adaptive_NVAR.ipynb
│   └── NG-RC.ipynb
├── data/
│   └── README.md
├── Datasets/
│   ├── East_Sea_Dataset.ipynb
│   ├── Yellow_Sea_Dataset.ipynb
│   ├── East_China_Sea_Dataset.ipynb
│   ├── PCA_Explained_Variance_Comparison.ipynb
│   └── Study_Regions.ipynb
├── East Sea/
│   ├── SST_normalize_East_Sea.ipynb
│   ├── Adaptive_NVAR_East_Sea.ipynb
│   └── NG-RC_East_Sea.ipynb
├── Yellow Sea/
│   ├── SST_normalize_Yellow_Sea.ipynb
│   ├── Adaptive_NVAR_Yellow_Sea.ipynb
│   └── NG-RC_Yellow_Sea.ipynb
└── East China Sea/
    ├── SST_normalize_East_China_Sea.ipynb
    ├── Adaptive_NVAR_East_China_Sea.ipynb
    └── NG-RC_East_China_Sea.ipynb
```

The PNG files in the regional directories are example/result figures generated by the notebooks.

## Requirements

The notebooks use Python scientific-computing and machine-learning packages including:

- NumPy
- SciPy
- Matplotlib
- scikit-learn
- PyTorch
- Optuna
- xarray
- copernicusmarine
- Cartopy (for the regional-map notebook)
- Jupyter

Install the documented Python dependencies with:

```bash
python -m pip install -r requirements.txt
```

A CUDA-capable GPU is optional. The Adaptive NVAR notebooks detect CUDA when it is available and otherwise use the CPU.

## Quick test

The repository includes a lightweight, self-contained test that does **not** require the full CMEMS/GLORYS12V1 dataset.

Run:

```bash
python examples/quick_test.py
```

The test creates a small synthetic SST-like field, performs PCA reduction, constructs the delay-embedded features used by the Adaptive NVAR formulation, trains a small neural nonlinear feature map, and performs a short autonomous forecast. It prints the forecast shape and error metrics and exits with a non-zero status if the basic workflow fails.

The quick test is intentionally small and is not intended to reproduce the scientific experiments reported in the manuscript.

## Data source

The regional experiments use sea-water potential temperature (`thetao`) from the Copernicus Marine Service product:

```text
cmems_mod_glo_phy_my_0.083deg_P1D-m
```

The dataset-preparation notebooks request approximately 0.083° horizontal resolution data at the surface level used in the study. The requested period in the current notebooks is:

```text
2012-08-19 through 2026-04-28
```

The regional spatial subsets are:

| Region | Longitude | Latitude |
|---|---:|---:|
| East Sea | 129.0–133.0°E | 35.0–40.0°N |
| Yellow Sea | 122.5–126.0°E | 34.0–38.0°N |
| East China Sea | 124.0–128.0°E | 28.0–33.0°N |

The original oceanographic NetCDF files are not redistributed in this repository. See [`data/README.md`](Datasets/README.md) for the data-acquisition and preprocessing workflow.

## Data preprocessing

The dataset notebooks:

1. obtain `thetao` from the Copernicus Marine Service;
2. extract the requested regional subset;
3. select the surface/depth level;
4. identify valid ocean grid cells using the first time slice;
5. remove land/invalid cells;
6. arrange the data as `(spatial features, time)`;
7. save the regional SST array as a NumPy file.

The normalization notebooks then use:

```text
warmup = 100 time steps
training = 3900 time steps
validation = 500 time steps
test = 500 time steps
```

For normalization, the mean and standard deviation are calculated from the warmup + training interval (the first 4000 time steps), and those statistics are applied to the complete time series.

## Corrected regional data workflow

Use the following mapping when preparing the regional arrays:

| Region | Raw NumPy file | Normalized file |
|---|---|---|
| East Sea | `east_sea_sst.npy` | `sst_normalized_5k.npy` |
| Yellow Sea | `yellow_sea_sst.npy` | `sst_normalized_5k.npy` |
| East China Sea | `east_china_sea_sst.npy` | `sst_normalized_5k.npy` |

**Important:** After downloading and preprocessing the datasets, place each regional NumPy file in its corresponding regional directory. Then run the appropriate normalization notebook (SST_normalize_*.ipynb) to generate the normalized dataset before running the forecasting models.

## PCA and forecasting workflow

For each region, the intended workflow is:

```text
CMEMS/GLORYS12V1 thetao
        │
        ▼
Regional subset
        │
        ▼
Ocean-mask / land removal
        │
        ▼
Regional NumPy array
        │
        ▼
Normalization
        │
        ▼
PCA fitted on the warmup + training period
        │
        ▼
One retained principal component
        │
        ▼
Adaptive NVAR / NG-RC
        │
        ▼
Autonomous forecasts
        │
        ▼
Evaluation at 10, 30, 60 and 90 time steps
```

The Adaptive NVAR notebooks use one retained PCA component:

```python
n_components = 1
```

and evaluate forecast horizons:

```python
[10, 30, 60, 90]
```

The Adaptive NVAR notebooks use Optuna for hyperparameter search. The search space currently includes:

- delay dimension `k`: 2, 5, 10, 20, 30, 40, 50;
- hidden dimension: 20, 50, 100;
- Adam learning rate: `1e-4` to `1e-1` on a logarithmic scale.

The notebooks currently specify 200 Optuna trials and multiple independent runs. Full experiments can therefore require substantially more computation than the quick test.

## Running the regional experiments

After preparing and normalizing a regional dataset, open the corresponding notebook in Jupyter.

### East Sea

```text
East Sea/SST_normalize_East_Sea.ipynb
East Sea/Adaptive_NVAR_East_Sea.ipynb
East Sea/NG-RC_East_Sea.ipynb
```

### Yellow Sea

```text
Yellow Sea/SST_normalize_Yellow_Sea.ipynb
Yellow Sea/Adaptive_NVAR_Yellow_Sea.ipynb
Yellow Sea/NG-RC_Yellow_Sea.ipynb
```

### East China Sea

```text
East China Sea/SST_normalize_East_China_Sea.ipynb
East China Sea/Adaptive_NVAR_East_China_Sea.ipynb
East China Sea/NG-RC_East_China_Sea.ipynb
```

The notebooks are research workflows and contain the detailed model implementation, training, hyperparameter optimization, forecasting, and visualization code.

## Reproducibility notes

- The complete regional oceanographic datasets are external and are not stored in this repository.
- The dataset acquisition notebooks document the Copernicus Marine dataset identifier and regional/time selections.
- The normalized SST arrays are intermediate products generated from the downloaded data.
- The quick test is independent of the external SST data and is intended to verify that the core PCA + Adaptive NVAR workflow can execute in a clean environment.
- Results from the full experiments may vary slightly with software versions, hardware, and numerical behavior unless the notebook's specified seeds and deterministic settings are used.

## License

This project is distributed under the MIT License. See [`LICENSE`](LICENSE).

## Contact

For questions concerning the implementation or manuscript, please contact: sherxonazimov94@pusan.ac.kr.
